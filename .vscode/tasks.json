{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Push to GitHub Pages",
      "type": "shell",
      "command": "BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo main); git fetch origin || true; git pull --rebase --autostash origin \"$BRANCH\" || true; git add -A; if ! git diff --cached --quiet; then git commit -m \"Quick update $(date '+%Y-%m-%d %H:%M:%S')\" && git push origin \"$BRANCH\"; else echo \"No changes to commit or push.\"; fi",
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": [],
      "presentation": { "reveal": "always", "panel": "dedicated", "clear": true }
    }
  ]
}